{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pip install -r requirements.txt && rembg i"
  },
  "deploy": {
    "startCommand": "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}